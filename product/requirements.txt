# JAXFEM Web 平台依赖(锁定本机已验证版本,jaxfem conda env)
dash==4.0.0
plotly==6.6.0
pyvista==0.46.5
vtk==9.5.2
flask==3.1.3
numpy==2.3.5
sectionproperties==3.10.2
matplotlib==3.10.8

# JAX(CUDA GPU 版)。jaxlib 的 CUDA 插件版本须与本机驱动匹配,
# 本机:RTX 4060 / CUDA 13.0 / Driver 580.x,按原环境安装方式保持一致:
#   pip install "jax[cuda]==0.9.0"
# 无 GPU 时可换纯 CPU 版:pip install jax==0.9.0
jax==0.9.0
