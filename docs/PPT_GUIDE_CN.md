# PPT 转换指南 — JAX EBE-PCG 项目

> 如何将 [TECH_REPORT_CN.md](TECH_REPORT_CN.md) 转换为 12 页演示文稿。
> 每页指定：**标题**、**文字内容**、**视觉元素**和**演讲者备注**。

---

## 第 1 页：封面

**标题**：基于 JAX EBE-PCG 的 GPU 并行有限元求解器

**副标题**：简支 BEAM4 梁可行性验证

**文字**：您的姓名 · 导师 · 日期 · 机构 Logo

**视觉**：通过 `post.py --save screenshot.png` 导出变形梁的三维渲染图（或打开 HTML 导出文件，在浏览器中截图）。

**演讲者备注**："本项目研究利用 JAX 的 vmap 向量化实现有限元分析的 GPU 加速。我们实现了逐元素 PCG 求解器，并在简支梁上进行了验证。"

---

## 第 2 页：问题与动机

**标题**：为什么需要 GPU 并行有限元？

**文字**：
- 桥梁抖振分析需要求解 **K u = F** 数千次
- 直接求解器：O(n³) — 对精细网格不可行
- 串行 CG：O(n²) 每次求解 — 对实时分析太慢
- **目标**：每次迭代成本为 O(n) 的 GPU 并行迭代求解器

**视觉**：[figures/architecture.pdf](figures/architecture.pdf)

**演讲者备注**："大跨度桥梁在风荷载下需要包含数千个时间步的时域分析。每个时间步都需求解一个大型线性系统。传统方法难以胜任。"

---

## 第 3 页：什么是 EBE-PCG？

**标题**：逐元素公式

**文字**：
- 经典 CG 需要全局刚度矩阵 K → O(n²) 内存
- EBE：将所有 CG 操作分解到单元级别
- 核心概念：**实向量 v^e**（仅自身）vs. **虚向量 v^(e)**（含邻居贡献）
- 内积：(r,r) = Σ_e (r^e)^T r^(e)
- 无需全局矩阵组装

**视觉**：[figures/ebe_principle.pdf](figures/ebe_principle.pdf)

**演讲者备注**："EBE 的突破性在于，每个 CG 操作——内积、矩阵向量乘——都可以分解为跨单元的求和。这意味着我们永远不需要构建全局刚度矩阵。每个单元只知道自身及其直接邻居。"

---

## 第 4 页：BEAM4 单元

**标题**：BEAM4 三维梁单元

**文字**：
- 2 节点 × 6 自由度/节点 = **12×12 刚度矩阵**
- 自由度：UX、UY、UZ、ROTX、ROTY、ROTZ
- 刚度分量：轴向 + 扭转 + XY 弯曲 + XZ 弯曲
- 截面属性通过 **sectionproperties** 计算（翘曲分析）

**视觉**：[figures/beam4_element.pdf](figures/beam4_element.pdf)

**演讲者备注**："我们使用 BEAM4——ANSYS 三维梁单元。每个节点有 6 个自由度。12×12 刚度矩阵结合了轴向、扭转和弯曲刚度。截面属性使用 sectionproperties 库进行完整翘曲分析计算。"

---

## 第 5 页：JAX 实现

**标题**：从串行循环到 GPU vmap

**文字（左列）**：
```
# NumPy：串行循环
for e in range(n_elem):
    Kp[e] = K_e[e] @ p_fake[e]
```
→ O(n_elem) × Python 开销

**文字（右列）**：
```
# JAX：vmap 并行
jax.vmap(lambda K, p: K @ p)(K_e, p_fake)
```
→ 所有单元在一个 GPU 内核中完成

**关键优化**：将稠密邻接矩阵 `(n×n) @ (n×6)` 替换为 O(n) 索引收集：`v_pad[neighbor_idx, :]`

**视觉**：[figures/benchmark_per_iter.pdf](figures/benchmark_per_iter.pdf)

**演讲者备注**："JAX 的关键特性是 vmap——它自动将函数在批量维度上向量化。我们将其应用于单元刚度操作，使所有单元在 GPU 上同时计算。我们还将 O(n²) 的稠密邻接乘法替换为 O(n) 的索引收集。"

---

## 第 6 页：挑战 1 — 精度崩溃

**标题**：大型模型为何失败：收敛陷阱

**文字**：
- 当 n_elem = 200，max_iter=500 → **96.7% 误差**！
- CG 在长时间停滞之后"突然"收敛
- 低频弯曲模态需要大量 Krylov 迭代才能捕捉
- **迭代次数在对角预条件子下约按 ~O(n^1.7) 增长**

**视觉**：[figures/convergence.pdf](figures/convergence.pdf) + 表格：

| n_elem | 所需迭代次数 | max_iter=500 |
|--------|:-----------:|:------------:|
| 10 | 11 | ✅ |
| 100 | 301 | ✅ |
| 200 | 1119 | ❌ 96.7% 误差 |
| 500 | 5765 | ❌ |

**演讲者备注**："最令人困惑的 bug：小模型完美运行，但大模型给出无意义的结果。根因是 CG 需要大量迭代来捕捉全局弯曲模态。对于 200 个单元，迭代 1-500 几乎无进展。解在第 600 次迭代左右突然出现。修复方案：基于问题规模的自适应 max_iter。"

---

## 第 7 页：挑战 2 — O(n²) 瓶颈

**标题**：邻接操作中隐藏的 O(n²)

**文字**：
- 原始 `real_to_fake` 使用了 `(n×n) @ (n×6)` 稠密矩阵乘法
- 对梁链而言：每个单元 ≤2 个邻居 → 99.9% 稀疏度
- **修复**：将稠密矩阵替换为索引数组 + 收集
- 结果：每次迭代时间恒定为 **~0.13ms，与 n_elem 无关**

**视觉**：[figures/benchmark_per_iter.pdf](figures/benchmark_per_iter.pdf)

| n_elem | 优化前 | 优化后 | 加速比 |
|--------|------:|------:|------:|
| 200 | 0.73ms | 0.34ms | 2.1× |
| 500 | 0.89ms | 0.13ms | **6.8×** |

**演讲者备注**："稠密邻接操作是一个无声的性能杀手。对于 500 个单元的梁链，我们在实际上仅需 500 次乘法的情况下计算了 250,000 次乘法。切换到索引收集彻底消除了这个问题。"

---

## 第 8 页：挑战 3 — 截面精度

**标题**：sectionproperties 扭转常数精度

**文字**：
- 默认网格对翘曲分析过于粗糙
- J 误差：6.7%（相对于 Saint-Venant 精确解）
- **修复**：网格尺寸从 `min(b,h)/20` → `min(b,h)/100`
- J 误差降至 **0.3%**

| 来源 | J (m⁴) | 误差 |
|------|--------|:---:|
| Saint-Venant | 4.698e-04 | — |
| ANSYS | 4.7e-04 | ~0% |
| sp（mesh=0.01） | 5.014e-04 | 6.7% |
| sp（mesh=0.002） | 4.712e-04 | 0.3% |

**演讲者备注**："对于面积和惯性矩等几何属性，网格尺寸无关紧要——它们可以直接从几何形状精确计算。但扭转常数 J 依赖于翘曲函数，需要精细网格才能准确求解。"

---

## 第 9 页：挑战 4 — 可视化

**标题**：使三维可视化适配 PPT

**文字**：
- **仅右侧子图出现参考网格** → 将 `show_shade` 移入每个子图循环
- **梁截面比例错误** → 相机设为侧视图（Y 轴）以展示 Z 方向高度
- **HTML 中色标精度丢失** → 数据缩放至 mm/MPa，显式 `clim=[min,max]`
- 输出：可嵌入 PPT 的自包含**交互式 HTML**（vtk.js），通过 Web Viewer 插入

**视觉**：使用 `post.py --save-html beam.html` 输出，截图用于幻灯片

**演讲者备注**："交互式三维可视化有助于传达结果。HTML 导出保留了旋转和缩放功能。对于 PPT，可通过 Office Web Viewer 嵌入，或使用 `window_size=(2400,1800)` 截取高分辨率截图。"

---

## 第 10 页：基准测试结果

**标题**：性能对比：JAX vs. NumPy vs. Direct

**文字**：
- **小模型（≤100 单元）**：GPU 开销占主导，JAX 更慢
- **交叉点约在 ~200 单元**：JAX 超越 NumPy EBE
- **500 单元**：JAX 比 NumPy EBE 快 **23 倍**
- **限制因素**：迭代次数，而非每次迭代速度

**视觉**：[figures/benchmark_time_cost.pdf](figures/benchmark_time_cost.pdf) + [figures/scaling.pdf](figures/scaling.pdf)

| n_elem | Direct | NP EBE | JAX EBE | JAX/NP |
|--------|-------:|-------:|--------:|-------:|
| 100 | 10ms | 190ms | 316ms | 0.6× |
| 200 | 39ms | 1198ms | 347ms | 3.5× |
| 500 | 369ms | 17026ms | 740ms | **23×** |

**演讲者备注**："基准测试结果清晰表明：GPU 在大规模时胜出。对于小规模问题，GPU 启动开销使 JAX 更慢。但在 500 单元时，23 倍的加速比非常显著。而且每次迭代成本是恒定的——剩余的增长来自迭代次数，这可以通过更好的预条件子来解决。"

---

## 第 11 页：未来工作

**标题**：超越对角预条件

**文字**：
- **当前瓶颈**：迭代次数以 O(n^1.7) 增长
- **根因**：对角预条件子无法处理条件数 κ ~ O(n⁴)
- **解决方案**：多重网格预条件子（MGCG）→ ~O(1) 迭代次数
  - 粗网格修正：P @ K_c⁻¹ @ P^T
  - 预期效果：n_elem=500 时从 6624 → ~30 迭代
- **扩展方向**：壳单元（SHELL181）、非线性分析、动态时间步进

**视觉**：两层多重网格 V 循环简图：

```
细网格   — 光滑 —\    /— 修正 — 光滑
                   V-cycle
粗网格 ———— K_c^{-1} ————
```

**演讲者备注**："解决迭代次数问题的终极方案是多重网格。通过求解问题的粗化版本并插值回修正量，我们可以将迭代次数从数千降至数十——与问题规模无关。这是我们下一个里程碑。"

---

## 第 12 页：总结与要点

**标题**：总结

**文字**：
1. ✅ **EBE-PCG on GPU** 可行且准确（经解析解验证）
2. ✅ **vmap 并行化**实现了每次迭代成本恒定（~0.13ms），与网格规模无关
3. ✅ **500 单元时 23 倍加速**（相对于 NumPy）
4. ⚠ **对角预条件子限制了收敛性** — 迭代次数以 O(n^1.7) 增长
5. 🔮 **下一步**：多重网格 PCG 以实现 O(1) 迭代

**关键数据**：
- BEAM4：12×12 刚度矩阵，6 DOF/节点
- J：4.712×10⁻⁴ m⁴（sectionproperties，0.3% 精度）
- 挠度：-2.2046 mm = PL³/(48EI)
- CG 500 单元：5765 次迭代，740 ms

**视觉**：导出 `post.py --save-html beam.html` 获取变形梁，截图作为幻灯片背景。

**演讲者备注**："总结：我们有了一个可运行的 GPU 并行有限元求解器。它是正确的，在大规模上显著快于串行实现，架构清晰。主要局限——迭代次数增长——通过多重网格方法有明确的解决路径。"

---

## 附录：图像生成命令

所有图片均可重新生成：

```bash
# 重新生成所有报告图表
python generate_figures.py

# 为幻灯片生成三维梁截图
python post.py --save slide_beam.png --window-size 2400 1800
python post.py --mises --save slide_mises.png --window-size 2400 1800

# 生成交互式 HTML（在浏览器中打开，截图用于 PPT）
python post.py --save-html interactive_beam.html
python post.py --mises --save-html interactive_mises.html
```

---

## 附录：PPT 设计建议

1. **统一配色**：使用图表中的 npg 配色（`#E64B35` 红、`#4DBBD5` 蓝、`#00A087` 绿）
2. **字体**：标题使用无衬线字体（Arial/Calibri），公式使用衬线字体
3. **公式渲染**：在 PowerPoint 中使用 LaTeX（`插入 → 公式`）
4. **幻灯片比例**：16:9 宽屏
5. **精简文字**：每页 ≤ 5 个要点，每个 ≤ 2 行
6. **图像质量**：以 dpi=200+ 导出 PDF 图表，或使用 `--window-size 2400 1800` 导出 PNG 截图
7. **交互演示**：答辩时在浏览器中打开 HTML 导出文件与幻灯片并列展示
8. **备用幻灯片**：添加 2-3 页附录，包含详细公式和数据表
